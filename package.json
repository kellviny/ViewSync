{
  "name": "view-sync-root",
  "version": "1.1.2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:desktop": "npm run dev --workspace=view-sync-desktop",
    "dev:inst": "cross-env VITE_APP_MODE=institutional NEXT_PUBLIC_APP_MODE=institutional npm run dev --workspace=view-sync-desktop",
    "dev:normal": "cross-env VITE_APP_MODE=normal NEXT_PUBLIC_APP_MODE=normal npm run dev --workspace=view-sync-desktop",
    "build:all": "npm run build --workspaces",
    "build": "npm run build --workspace=view-sync-desktop",
    "build:inst:win": "cross-env VITE_APP_MODE=institutional NEXT_PUBLIC_APP_MODE=institutional npm run build:win --workspace=view-sync-desktop",
    "build:normal:win": "cross-env VITE_APP_MODE=normal NEXT_PUBLIC_APP_MODE=normal npm run build:win --workspace=view-sync-desktop",
    "build:inst:mac:arm64": "cross-env VITE_APP_MODE=institutional NEXT_PUBLIC_APP_MODE=institutional npm run build:mac:arm64 --workspace=view-sync-desktop",
    "build:normal:mac:arm64": "cross-env VITE_APP_MODE=normal NEXT_PUBLIC_APP_MODE=normal npm run build:mac:arm64 --workspace=view-sync-desktop",
    "version:patch": "npm version patch --workspaces --include-workspace-root --no-git-tag-version",
    "version:minor": "npm version minor --workspaces --include-workspace-root --no-git-tag-version",
    "version:major": "npm version major --workspaces --include-workspace-root --no-git-tag-version"
  },
  "optionalDependencies": {
    "lightningcss-darwin-arm64": "1.32.0",
    "lightningcss-darwin-x64": "1.32.0",
    "lightningcss-linux-x64-gnu": "1.32.0",
    "lightningcss-win32-x64-msvc": "1.32.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "puppeteer": "^24.42.0"
  }
}
